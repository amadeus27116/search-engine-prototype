# search-engine-prototype

A simple, AI generated prototype of a search engine for a ecommerce website's product listings. This project serves as
a proof-of-concept to demonstrate the structure and organization for how a real search engine, to be developed later,
will actually function once implemented.

## Installation Guide

### Linux

#### Clone the repository
```bash
git clone https://github.com/amadeus27116/search-engine-prototype.git
cd search-engine-prototype
```

#### Create the virtual environment and install dependencies
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### Start the app
```bash
python app.py
```

#### Note
The app will display a url along the lines of "`http://127.0.0.1:<port-number>`". To view the app, enter the url into
your browser to run the view the home page.

#### Deactivating the app
When finished running the app, type the following command to deactivate the virtual environment:
```bash
deactivate
```

### Windows

#### Clone the repository
Using the Git CMD terminal type the following commands:
```bat
git clone https://github.com/amadeus27116/search-engine-prototype.git
cd search-engine-prototype
```

#### Create the virtual environment and install dependencies
You can switch over to a normal windows command prompt at this point
```bat
python -m venv venv
venv\Scripts\activate.bat
pip install -r requirements.txt
```

#### Start the app
```bat
python app.py
```

#### Note
The app will display a url along the lines of "`http://127.0.0.1:<port-number>`". To view the app, enter the url into
your browser to run the view the home page.

#### Deactivating the app
When finished running the app, type the following command to deactivate the virtual environment:
```bat
deactivate
```

